# Developer 2 dev/test dependencies (CPU) — for unit-testing the JEPA + analysis half
# before core-frozen. The full pinned repro env is environment.yml (Dev 1 / CP3).
#
#   python3.12 -m venv --system-site-packages .venv
#   .venv/bin/pip install -r requirements-dev.txt
#   .venv/bin/python -m pytest tests/ -q
#
torch>=2.2
numpy>=1.26
pandas>=2.0
scikit-learn>=1.3
scipy>=1.11
matplotlib>=3.7
pyarrow>=12
pytest>=7
