__pycache__/
*.py[cod]
.ipynb_checkpoints/
.venv/
env/
# large/shared artifacts live in DATA_ROOT (outside the repo); ignore any local copies
data/
runs/
checkpoints/
*.h5ad
*.parquet
!results/*.csv
# the explorer's small, committed, verified JSON (source:"real") is NOT a DATA_ROOT artifact
!explorer/data/
.DS_Store
