"""figures — demo figure generation for the CD4+ perturbation project (§12).

Developer 2 owns all three demo figures + the biology annotation figure. Import from
``figures.make_figures``. Rendering uses the Agg backend (headless) so it runs on the
GPU box and in CI without a display.
"""
