"""core.models — model implementations (baselines, causal transformer, JEPA).

Nothing is imported here at package level so that importing one model does not drag
in torch/tabpfn for a job that does not need them.
"""
